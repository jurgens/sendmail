.page-header
  %h1 Mailbox

- if @messages.empty?
  %p
    No messages sent.
    = link_to 'Compose'

- else
  %table.table
    %thead
      %tr
        %th.to To
        %th.subject Subject
        %th.date Date
    - @messages.each do |message|
      %tr
        %td= message.to
        %td= message.subject
        %td= time_ago_in_words message.created_at


